{
  "name": "playwrightWeb",
  "version": "1.0.0",
  "scripts": {
    "test": "cucumber-js --require-module ts-node/register --require src/**/*.ts --format json:reports/cucumber_report.json src/features/*.feature",
    "report": "node generateReport.js"
  },
  "dependencies": {
    "@cucumber/cucumber": "^11.0.1",
    "@playwright/test": "^1.46.0",
    "playwrightWeb": "file:",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^16.0.0",
    "cucumber-html-reporter": "^7.2.0",
    "ts-node": "^10.0.0",
    "typescript": "^4.0.0"
  }
}
